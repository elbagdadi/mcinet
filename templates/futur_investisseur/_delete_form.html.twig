<form method="post" action="{{ path('futur_investisseur_delete', {'id': futur_investisseur.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ futur_investisseur.id) }}">
    <button class="btn">Delete</button>
</form>
