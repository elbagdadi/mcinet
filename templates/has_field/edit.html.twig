{% extends 'base.html.twig' %}

{% block title %}Edit HasField{% endblock %}

{% block body %}
    <h1>Edit HasField</h1>

    {{ include('has_field/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('has_field_index') }}">back to list</a>

    {{ include('has_field/_delete_form.html.twig') }}
{% endblock %}
