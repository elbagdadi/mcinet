{% extends 'base.html.twig' %}

{% block title %}NatureDuProjet{% endblock %}

{% block body %}
    <h1>NatureDuProjet</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ nature_du_projet.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ nature_du_projet.title }}</td>
            </tr>
            <tr>
                <th>Slug</th>
                <td>{{ nature_du_projet.slug }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('nature_du_projet_index') }}">back to list</a>

    <a href="{{ path('nature_du_projet_edit', {'id': nature_du_projet.id}) }}">edit</a>

    {{ include('nature_du_projet/_delete_form.html.twig') }}
{% endblock %}
