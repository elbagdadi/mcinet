<form method="post" action="{{ path('details_estimation_delete', {'id': details_estimation.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ details_estimation.id) }}">
    <button class="btn">Delete</button>
</form>
